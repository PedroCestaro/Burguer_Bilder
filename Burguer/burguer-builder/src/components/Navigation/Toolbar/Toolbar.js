import React from 'react';
import './styles.css'

const toolbar = (props) =>(
    <header className="toolbar">
        <div>MENU</div>
        <div>LOGO</div>
        <nav>
           ...
        </nav>
    </header>
);

export default toolbar;